# @gilvandovieira/cli

The deliberately narrow Thor migration CLI. This release provides `thor init`
and `thor create <name>`. Database-connected migration commands are available
through the programmatic `@gilvandovieira/thor/migrate` API and are not exposed
by this binary yet.

Node.js 22 or newer is supported. Run `thor --help` for the shipped command set.
